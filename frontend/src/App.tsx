/**
 * App-Komponente – wird aktuell nicht direkt verwendet,
 * da das Routing über AppRouter läuft.
 * Kann später als Layout-Wrapper genutzt werden.
 */
export default function App() {
  return (
    <div>
      <h1>Astra</h1>
    </div>
  );
}
